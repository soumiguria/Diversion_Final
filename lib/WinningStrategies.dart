import 'package:flutter/material.dart';

class WinningStrategies extends StatelessWidget {
  const WinningStrategies({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Text('Winning Strategy'),
      ),
    );
  }
}