import 'package:flutter/material.dart';

class CompetitorsPage extends StatelessWidget {
  const CompetitorsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Text('Competitors Page'),
      ),
    );
  }
}