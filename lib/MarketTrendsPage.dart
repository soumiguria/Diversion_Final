import 'package:flutter/material.dart';

class MarketTrendsPage extends StatelessWidget {
  const MarketTrendsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Text('MarketTrendsPage'),
      ),
    );
  }
}