import 'package:flutter/material.dart';

class MarketPotentialPage extends StatelessWidget {
  const MarketPotentialPage({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Text('MarketPotentialPage'),
      ),
    );
  }
}